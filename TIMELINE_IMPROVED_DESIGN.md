# Timeline Comparison - 改进版设计

## 🎯 核心问题

**当前设计的问题**：
- ❌ 没有明确的"输入"（prompt/context）
- ❌ 没有明确的"输出"（生成的tokens）
- ❌ 看不出迭代过程（输出→下一轮输入）
- ❌ 缺少"时间流逝"的感觉

**改进目标**：
- ✅ 明确显示每一步的输入和输出
- ✅ 展示迭代过程
- ✅ 体现时间轴概念

---

## 🔄 改进方案：加入输入输出流

### 整体思路

每个方法都要展示：
```
[Prompt/Context] → [Processing] → [Output] → [Next Input] → ...
```

关键元素：
1. **Prompt box**（最左边）- 灰色，表示初始输入
2. **Processing boxes**（中间）- 彩色，表示计算过程
3. **Output tokens**（框的右侧）- 绿色，表示生成的token
4. **Context growth**（横向扩展）- 显示context逐渐增长

---

## 🔵 Part 1: Autoregressive (改进版)

### 原版（问题）
```
[LLM→t₁] → [LLM→t₂] → [LLM→t₃] → [LLM→t₄] → [LLM→t₅]
```
**问题**：看不出输入输出，不知道context在哪里

### 改进版
```
┌────────┐    ┌─────────────────────────────────────────────────────┐
│ Prompt │ →  │           Autoregressive Generation                 │
│  "..."  │    │                                                     │
└────────┘    │ [p] → [LLM] → t₁ → [LLM] → t₂ → [LLM] → t₃ → ...   │
               │  ↑     ↓           ↑     ↓           ↑     ↓       │
               │ input output     input output     input output     │
               └─────────────────────────────────────────────────────┘
```

**或者更清楚的版本**：
```
Prompt: [The cat sat on]

Step 1:  [The cat sat on] → LLM → [the]
Step 2:  [The cat sat on the] → LLM → [mat]  
Step 3:  [The cat sat on the mat] → LLM → [.]
...

每一步：
  输入 = Prompt + 之前生成的所有tokens
  输出 = 1个新token
  下一步的输入 = 当前输入 + 当前输出
```

**视觉表现**：
- 用**嵌套的方框**表示context不断增长
- 或者用**颜色深浅**表示：旧的token浅色，新的token深色

---

## 🟢 Part 2: Linear Speculative (改进版)

### 原版（问题）
```
[Draft顺序] ⇒ [LLM并行验证] ⇒ [继续Draft]
```
**问题**：看不出draft的candidates是什么，verified后接受了哪些

### 改进版
```
Context: [The cat sat on]

┌──────────────────────────┐
│ Iteration 1              │
│                          │
│ 1) Draft generates:      │
│    [the, mat, and, ...] ←draft candidates (6 tokens)
│                          │
│ 2) LLM verifies:         │
│    [the✓, mat✓, and✗]  ←verify in parallel
│                          │
│ 3) Accept: [the, mat]   ←only accept verified prefix
│                          │
└──────────────────────────┘

New Context: [The cat sat on the mat]

┌──────────────────────────┐
│ Iteration 2              │
│ ...                      │
└──────────────────────────┘
```

**视觉表现**：
- **Draft box**内部显示6个candidates（灰色虚线框）
- **Verify box**用绿色✓标注接受的，红色✗标注拒绝的
- **箭头**从verified tokens指向下一轮的input

**关键改进**：
```
Input:  [Context]
  ↓
Draft:  [Context] → generates → [t₁, t₂, t₃, t₄, t₅, t₆]
  ↓
Verify: [Context + candidates] → LLM parallel check → ✓ t₁, t₂ | ✗ t₃...
  ↓
Output: [Context + t₁ + t₂]  (accept 2 tokens)
```

---

## 🟣 Part 3: DynaTree (改进版)

### 原版（问题）
```
[Draft Tree] ⇒ [TreeVerify] ⇒ [Next Tree]
```
**问题**：树的candidates不清楚，accepted path不明显

### 改进版

```
Context: [The cat sat on]

┌──────────────────────────────────────┐
│ Iteration 1                          │
│                                      │
│ 1) Draft generates tree:            │
│                                      │
│        [the]                         │ ← Level 1
│       /  |  \                        │
│     mat and .                       │ ← Level 2
│     / |  |  | \                     │
│   ...  ...  ...                     │ ← Level 3
│   (20-30 nodes total)               │
│                                      │
│ 2) LLM verifies entire tree:        │
│    Checks all paths in parallel     │
│                                      │
│ 3) Find longest valid path:         │
│    [the] → [mat] → [.] ✓✓✓         │ ← This path matches LLM
│                                      │
│ Accept 3 tokens: the mat .          │
│                                      │
└──────────────────────────────────────┘

New Context: [The cat sat on the mat.]

┌──────────────────────────────────────┐
│ Iteration 2                          │
│ ...                                  │
└──────────────────────────────────────┘
```

**视觉表现**：
- **树的节点**用不同颜色：
  - 灰色：所有candidates
  - 绿色加粗：accepted path
  - 红色虚线：rejected paths
- **箭头**明确指向accepted path
- **Context box**清楚显示在最左边

**关键改进**：
```
Input:  [Context]
  ↓
Draft Tree:  
         t₁
        /│\
       t₂ t₃ t₄
      /│  │  │\
     ...  ...  ... (20-30 nodes)
  ↓
Verify: LLM checks all nodes in one pass
  ↓
Select: Find longest valid path → [t₁→t₂→t₅] ✓
  ↓
Output: [Context + t₁ + t₂ + t₅]  (accept 3 tokens)
```

---

## 🎨 改进的核心视觉元素

### 1. **Context Box**（输入框）

每个方法的最左边都要有：
```
┌──────────┐
│  Prompt  │  或  │ Context │
│  + Prev  │      │ [p+t₁+t₂]│
└──────────┘
```
- 灰色背景
- 显示当前的输入context
- 每轮迭代后会更新

### 2. **Generation Box**（生成框）

中间的处理框要明确显示：
```
┌─────────────────┐
│  Input:  [...]  │ ← 输入
│     ↓           │
│  Process: LLM   │ ← 处理
│     ↓           │
│  Output: t₁,t₂  │ ← 输出
└─────────────────┘
```

### 3. **Token Flow**（token流）

用颜色和箭头表示：
```
灰色 [existing context] → 绿色 [new tokens] → 下一轮的灰色 [updated context]
```

### 4. **Verification Status**（验证状态）

对于Speculative方法：
```
Draft candidates: [t₁] [t₂] [t₃] [t₄] [t₅] [t₆]
                   ✓    ✓    ✗    ✗    ✗    ✗
Verified result:  accept ─┘    └─ reject
```

---

## 📐 推荐的布局方式

### 方式A：横向时间轴（推荐）

```
时间 →

[Initial]     [Iter 1]        [Iter 2]        [Iter 3]
  ↓             ↓               ↓               ↓
Prompt    → Process → t₁ → Process → t₂ → Process → t₃
            (input)  (out)  (input)  (out)  (input)  (out)
```

### 方式B：垂直分层

```
Layer 1: Input     [Prompt] → [p+t₁] → [p+t₁+t₂] → ...
                      ↓         ↓          ↓
Layer 2: Process    [LLM]    [LLM]      [LLM]    ...
                      ↓         ↓          ↓
Layer 3: Output     [t₁]     [t₂]       [t₃]     ...
```

### 方式C：对话式（最直观）⭐

```
(a) Autoregressive:

   You:  "The cat sat on"
   LLM:  "the"           (1 token, 1 step)
   You:  "The cat sat on the"
   LLM:  "mat"           (1 token, 1 step)
   ...

(b) Linear Speculative:

   You:  "The cat sat on"
   Draft: "the mat and ..."  (6 candidates)
   LLM:  "the mat" ✓✓, "and..." ✗✗  (verify all, accept 2)
   You:  "The cat sat on the mat"
   ...

(c) DynaTree:

   You:  "The cat sat on"
   Draft Tree:  
         the
        /│\
       mat and .
      ...multiple paths...
   LLM:  "the→mat→." ✓✓✓  (verify tree, accept path of 3)
   You:  "The cat sat on the mat."
   ...
```

**这个最直观！** 像对话一样，清楚展示输入输出。

---

## 🎯 最终建议

### 推荐设计：对话式 + 数据流

结合两者优点：

```
╔════════════════════════════════════════════════════════════╗
║ (a) Autoregressive Decoding                                ║
╠════════════════════════════════════════════════════════════╣
║                                                            ║
║  Context: [The cat sat on]                                ║
║     ↓                                                      ║
║  ┌─────────┐    ┌─────────┐    ┌─────────┐               ║
║  │ LLM     │ →  │ LLM     │ →  │ LLM     │  → ...        ║
║  │ Input:p │    │ Input:  │    │ Input:  │               ║
║  │ Output:t│    │ p+t₁    │    │ p+t₁+t₂ │               ║
║  │     ↓   │    │ Output: │    │ Output: │               ║
║  │    [t₁] │    │   [t₂]  │    │   [t₃]  │               ║
║  └─────────┘    └─────────┘    └─────────┘               ║
║                                                            ║
║  Result: Generate 1 token per step (serial)               ║
╠════════════════════════════════════════════════════════════╣
║ (b) Linear Speculative Decoding                           ║
╠════════════════════════════════════════════════════════════╣
║                                                            ║
║  Context: [The cat sat on]                                ║
║     ↓                                                      ║
║  ┌────────────────┐      ┌───────────────┐               ║
║  │ Draft Model    │  →   │ LLM Verify    │               ║
║  │ Input: p       │      │ Input: p +    │               ║
║  │ Output:        │      │  [t₁..t₆]     │               ║
║  │ [t₁,t₂,t₃,     │      │               │               ║
║  │  t₄,t₅,t₆]     │      │ Verify each:  │               ║
║  │ (6 candidates) │      │  t₁✓ t₂✓      │               ║
║  └────────────────┘      │  t₃✗ ...✗     │               ║
║                          │               │               ║
║                          │ Accept: [t₁,t₂]│              ║
║                          └───────────────┘               ║
║     ↓                                                      ║
║  New Context: [The cat sat on the mat]                   ║
║                                                            ║
║  Result: Generate 2 tokens per step (2-phase)            ║
╠════════════════════════════════════════════════════════════╣
║ (c) DynaTree (Tree-based Speculative)                    ║
╠════════════════════════════════════════════════════════════╣
║                                                            ║
║  Context: [The cat sat on]                                ║
║     ↓                                                      ║
║  ┌────────────────┐      ┌───────────────┐               ║
║  │ Draft Tree     │  →   │ LLM TreeVerify│               ║
║  │ Input: p       │      │ Input: p +    │               ║
║  │ Output:        │      │  [tree nodes] │               ║
║  │      t₁        │      │               │               ║
║  │     /│\        │      │ Check all     │               ║
║  │   t₂ t₃ t₄    │      │  paths:       │               ║
║  │   /│  │  │\    │      │               │               ║
║  │  t₅... ... ...│      │ Find longest: │               ║
║  │ (20-30 nodes)  │      │  t₁→t₂→t₅ ✓✓✓ │              ║
║  └────────────────┘      │               │               ║
║                          │ Accept: 3 tok │               ║
║                          └───────────────┘               ║
║     ↓                                                      ║
║  New Context: [The cat sat on the mat.]                  ║
║                                                            ║
║  Result: Generate 3+ tokens per step (tree parallel)     ║
╚════════════════════════════════════════════════════════════╝
```

**关键改进**：
1. ✅ 每个方法都有明确的 **Context → Process → Output**
2. ✅ 清楚显示每一步的**输入**和**输出**
3. ✅ 用箭头表示**数据流**
4. ✅ 在框内明确标注 "Input:" 和 "Output:"
5. ✅ 用颜色区分：输入（灰）、处理（彩）、输出（绿）
6. ✅ 每个方法最后有 "Result:" 总结

---

## 💡 具体实现建议

### 在PowerPoint中实现

1. **Context Box**（最左边）
   - 灰色矩形
   - 文字："Context: [The cat sat on]"

2. **Processing Box**（中间）
   - 彩色矩形（蓝/绿/紫）
   - 内部分成三层：
     ```
     ┌─────────────┐
     │ Input: [p]  │ ← 小字
     │             │
     │   [LLM]     │ ← 大字
     │             │
     │ Output: t₁  │ ← 小字
     └─────────────┘
     ```

3. **Output Tokens**（框外，绿色）
   - 用绿色的小圆圈或方框
   - 标注 "[t₁]", "[t₂]" 等

4. **Data Flow Arrows**
   - 从Context到Process：粗箭头
   - 从Process到Output：绿色箭头
   - 从Output到Next Input：虚线箭头

5. **对于Speculative方法**
   - Draft box内部用虚线框表示candidates
   - Verify box内部用✓✗表示验证结果

---

## 🎨 颜色语义

| 元素 | 颜色 | 含义 |
|------|------|------|
| Context Box | 灰色 (#E0E0E0) | 输入（已有的）|
| Processing Box | 蓝/绿/紫（鲜艳）| 计算过程 |
| Output Token | 绿色 (#4CAF50) | 新生成的 |
| Accepted | 深绿 + ✓ | 被接受的 |
| Rejected | 红色 + ✗ | 被拒绝的 |
| Data Flow | 黑色箭头 | 数据流动 |

---

## ✨ 关键要点

**改进后，读者能看出**：

1. **AR**: 
   - Input每次都要包含所有历史
   - Output每次只有1个token
   - 迭代多次

2. **Linear**:
   - Input一样（context）
   - Draft生成多个candidates
   - Verify接受部分
   - Output 2-3个tokens

3. **DynaTree**:
   - Input一样（context）
   - Draft生成树（多条路径）
   - Verify接受最长路径
   - Output 3-6个tokens

**对比一目了然！** 谁的输入输出比率高，谁就更efficient！

---

你觉得这个改进方案怎么样？要不要我帮你重新写一个更详细的绘图指南？

